general {
        output_format = "i3bar"
        colors = true
        interval = 5
}

order += "cpu_usage"
order += "memory"

memory {
  format = "memory: (%used) / %total"
}

cpu_usage {
  format = "cpu: %usage"
}
